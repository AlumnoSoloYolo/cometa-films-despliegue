# ===== CONFIGURACIÓN DEL PROYECTO =====
# Copia este archivo como .env y completa las variables

# Puerto del backend
PORT=3000

# JWT Secret (genera uno seguro)
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui

# MongoDB URI
MONGODB_URI=mongodb://localhost:27017/tu_base_de_datos
# O MongoDB Atlas: mongodb+srv://usuario:password@cluster.mongodb.net/database

# PayPal (para pagos premium)
PAYPAL_CLIENT_ID=tu_paypal_client_id
PAYPAL_CLIENT_SECRET=tu_paypal_client_secret

# URL del frontend (IP pública de tu servidor)
FRONTEND_URL=http://TU_IP_PUBLICA_AQUI

# TMDB API Key (para información de películas)
TMDB_API_KEY=tu_tmdb_api_key

# ===== INSTRUCCIONES =====
# 1. Copia este archivo: cp .env-plantilla .env  
# 2. Completa todas las variables
# 3. Ejecuta: docker-compose up --build -d
